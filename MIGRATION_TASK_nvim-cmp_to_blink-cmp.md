# nvim-cmp 迁移到 blink.cmp 任务清单

## 📋 任务概览

将配置从 `nvim-cmp` 系列插件完全迁移到 `blink.cmp`，这是一个性能更优的现代化补全插件。

**预计影响文件数量**: 5-8 个文件  
**预计所需时间**: 15-20 分钟  
**风险等级**: 🟡 中等（需要测试补全、代码片段、Copilot 集成）

---

## 📊 当前配置分析

### 当前使用的 nvim-cmp 相关插件

| 插件名称 | 作用 | 是否有 blink.cmp 替代 |
|---------|------|---------------------|
| `hrsh7th/nvim-cmp` | 补全引擎核心 | ✅ blink.cmp 本身 |
| `hrsh7th/cmp-nvim-lsp` | LSP 补全源 | ✅ 内置 |
| `hrsh7th/cmp-buffer` | 缓冲区补全源 | ✅ 内置 |
| `hrsh7th/cmp-path` | 路径补全源 | ✅ 内置 |
| `hrsh7th/cmp-cmdline` | 命令行补全源 | ✅ 内置 |
| `zbirenbaum/copilot-cmp` | Copilot 补全源 | ✅ 通过 blink-cmp-copilot |
| `garymjr/nvim-snippets` | 代码片段引擎 | ✅ 支持（使用 vim.snippet） |
| `rafamadriz/friendly-snippets` | 代码片段库 | ✅ 支持 |

### 当前配置文件

1. **主配置文件**: `lua/plugins/nvim-cmp.lua` (190 行)
2. **工具函数**: `lua/utils/cmp.lua` (203 行)
3. **Copilot 配置**: `lua/plugins/copilot-chat.lua` (依赖 copilot.lua 和 copilot-cmp)
4. **LSP 配置**: `lua/plugins/lsp.lua` (使用 cmp-nvim-lsp 的 capabilities)
5. **状态栏**: `lua/plugins/lualine.lua` (显示 Copilot 状态)

---

## ✅ 任务清单

### 阶段 1: 准备工作

- [ ] **任务 1.1**: 备份当前配置
  - 文件: 所有涉及的配置文件
  - 操作: 创建备份目录或 git commit
  
- [ ] **任务 1.2**: 下载并阅读文档（已完成）
  - ✅ nvim-cmp 文档
  - ✅ blink.cmp 文档
  - ✅ blink.cmp 配置参考

### 阶段 2: 移除 nvim-cmp 相关插件

- [ ] **任务 2.1**: 移除 `lua/plugins/nvim-cmp.lua` 文件
  - 操作: 删除或重命名为 `.bak`
  - 涉及的依赖插件:
    - `hrsh7th/nvim-cmp`
    - `hrsh7th/cmp-nvim-lsp`
    - `hrsh7th/cmp-buffer`
    - `hrsh7th/cmp-path`
    - `hrsh7th/cmp-cmdline`
    - `zbirenbaum/copilot-cmp`

- [ ] **任务 2.2**: 清理 `lua/utils/cmp.lua` 文件
  - 选项 A: 完全删除（推荐，因为 blink.cmp 不需要这些工具函数）
  - 选项 B: 重命名为 `.bak` 保留

### 阶段 3: 安装 blink.cmp

- [ ] **任务 3.1**: 创建新的 blink.cmp 配置文件
  - 文件路径: `lua/plugins/blink-cmp.lua`
  - 基础配置结构:
    ```lua
    return {
      'saghen/blink.cmp',
      dependencies = { ... },
      version = '1.*',
      opts = { ... }
    }
    ```

- [ ] **任务 3.2**: 配置基本选项
  - 启用配置
  - 键盘映射（模仿当前行为）
  - 补全触发器
  - 补全列表行为

- [ ] **任务 3.3**: 配置补全源
  - LSP 源（内置，自动启用）
  - Buffer 源（内置，自动启用）
  - Path 源（内置，自动启用）
  - Snippets 源（内置，自动启用）
  - Lazydev 源（Lua 开发增强）

### 阶段 4: 代码片段集成

- [ ] **任务 4.1**: 配置 snippets 引擎
  - 使用 `vim.snippet` 作为引擎（当前配置已在使用）
  - 配置 friendly-snippets 加载
  
- [ ] **任务 4.2**: 保留 nvim-snippets 插件
  - 文件: `lua/plugins/nvim-cmp.lua` 中的 nvim-snippets 依赖
  - 注意: blink.cmp 支持 friendly-snippets，需确认是否保留 nvim-snippets

### 阶段 5: Copilot 集成

- [ ] **任务 5.1**: 安装 Copilot 社区源
  - 选项 A: `giuxtaposition/blink-cmp-copilot` （推荐）
  - 选项 B: `fang2hou/blink-copilot`
  
- [ ] **任务 5.2**: 配置 Copilot 源
  - 添加到 sources.providers
  - 设置优先级（group_index = 1, priority = 100）
  - 配置图标

- [ ] **任务 5.3**: 保留 copilot.lua 插件
  - 文件: `lua/plugins/copilot-chat.lua`
  - 操作: 只移除 copilot-cmp 依赖，保留 copilot.lua

### 阶段 6: LSP 集成

- [ ] **任务 6.1**: 更新 LSP capabilities 配置
  - 文件: `lua/plugins/lsp.lua`
  - 当前代码:
    ```lua
    local capabilities = require('cmp_nvim_lsp').default_capabilities()
    ```
  - 替换为:
    ```lua
    local capabilities = require('blink.cmp').get_lsp_capabilities()
    ```

- [ ] **任务 6.2**: 检查 LSP 相关配置
  - 确保 auto_brackets 功能正常
  - 确保 snippetSupport 正确设置

### 阶段 7: 键盘映射迁移

当前 nvim-cmp 的键盘映射需要转换为 blink.cmp 格式：

- [ ] **任务 7.1**: 迁移基础键映射
  - `<C-b>` - 向上滚动文档
  - `<C-f>` - 向下滚动文档
  - `<ESC>` - 取消补全
  - `<CR>` - 确认选择
  - `<Tab>` - 确认或跳转或唤出补全
  - `<F13>` - 手动触发补全

- [ ] **任务 7.2**: 处理特殊逻辑
  - Copilot suggestion 可见时接受建议
  - Snippet 跳转
  - 光标前有字符时唤出补全
  - 创建撤销点

### 阶段 8: UI 配置

- [ ] **任务 8.1**: 配置补全菜单
  - 边框样式（当前使用 bordered）
  - 窗口高亮
  - 最大高度和宽度

- [ ] **任务 8.2**: 配置文档窗口
  - 自动显示（当前为 false）
  - 延迟时间（当前 500ms）
  - 边框样式

- [ ] **任务 8.3**: 配置显示格式
  - 图标显示（kind_icon）
  - 标签显示（label）
  - 来源显示（自定义图标）

### 阶段 9: 命令行补全

- [ ] **任务 9.1**: 配置 cmdline 模式
  - 文件路径: 同一配置文件的 `cmdline` 选项
  - 配置 `:`, `/`, `?` 的补全源

### 阶段 10: 清理和验证

- [ ] **任务 10.1**: 移除未使用的工具函数
  - 检查 `lua/utils/init.lua` 中是否有对 `utils.cmp` 的引用
  - 移除或注释掉相关代码

- [ ] **任务 10.2**: 更新 lazy-lock.json
  - 操作: 启动 Neovim 后自动更新
  - 验证: 检查是否移除了 nvim-cmp 相关插件

- [ ] **任务 10.3**: 清理 mason 安装
  - 可选: 已安装的补全相关工具可能不需要了

### 阶段 11: 测试验证

- [ ] **任务 11.1**: 基础补全测试
  - 测试 LSP 补全（Lua, Python, TypeScript 等）
  - 测试 Buffer 补全
  - 测试 Path 补全
  - 测试命令行补全

- [ ] **任务 11.2**: 代码片段测试
  - 测试 friendly-snippets 是否加载
  - 测试代码片段展开
  - 测试 Tab 跳转

- [ ] **任务 11.3**: Copilot 测试
  - 测试 Copilot 补全是否出现
  - 测试 Copilot 优先级
  - 测试 Copilot inline suggestion（如果仍在使用）
  - 测试 CopilotChat 是否正常

- [ ] **任务 11.4**: 特殊功能测试
  - 测试自动括号
  - 测试 ghost text
  - 测试文档自动显示
  - 测试 fuzzy 匹配

- [ ] **任务 11.5**: 性能测试
  - 测试启动速度
  - 测试补全响应速度
  - 测试大文件补全

---

## 🔧 详细配置转换对照

### nvim-cmp 到 blink.cmp 配置映射

| nvim-cmp 配置 | blink.cmp 配置 | 说明 |
|--------------|---------------|------|
| `completion.completeopt` | 内置处理 | 自动处理 |
| `preselect` | `completion.list.selection.preselect` | 预选第一项 |
| `mapping` | `keymap` | 键盘映射 |
| `sources` | `sources.default` + `sources.providers` | 补全源配置 |
| `window.completion` | `completion.menu` | 补全菜单窗口 |
| `window.documentation` | `completion.documentation` | 文档窗口 |
| `formatting` | `completion.menu.draw` | 显示格式 |
| `experimental.ghost_text` | `completion.ghost_text` | 幽灵文本 |
| `snippet.expand` | `snippets.expand` | 代码片段展开 |

### 键盘映射转换

| 功能 | nvim-cmp | blink.cmp |
|-----|----------|-----------|
| 向上滚动文档 | `cmp.mapping.scroll_docs(-4)` | `scroll_documentation_up` |
| 向下滚动文档 | `cmp.mapping.scroll_docs(4)` | `scroll_documentation_down` |
| 取消补全 | `cmp.mapping.abort()` | `cancel` + `fallback` |
| 确认选择 | `cmp.mapping.confirm()` | `accept` |
| 手动触发 | `cmp.mapping.complete()` | `show` |
| 选择上一项 | `cmp.mapping.select_prev_item()` | `select_prev` |
| 选择下一项 | `cmp.mapping.select_next_item()` | `select_next` |
| Snippet 跳转 | `vim.snippet.jump()` | `snippet_forward` / `snippet_backward` |

---

## 📝 需要创建的新配置文件结构

### `lua/plugins/blink-cmp.lua`

```lua
return {
  'saghen/blink.cmp',
  version = '1.*',
  
  dependencies = {
    -- Copilot 源（选择其一）
    { 'giuxtaposition/blink-cmp-copilot' },
    -- 或者 'fang2hou/blink-copilot'
    
    -- 代码片段（保留现有配置）
    'rafamadriz/friendly-snippets',
  },
  
  event = 'InsertEnter',
  
  opts = {
    -- 详细配置...
  },
  
  -- 可选: 配置完成后的初始化
  config = function(_, opts)
    require('blink.cmp').setup(opts)
    -- 其他初始化代码...
  end,
}
```

---

## ⚠️ 注意事项和风险

### 可能的问题

1. **Copilot 集成**
   - 风险: Copilot inline suggestion 可能需要调整
   - 解决: 需要测试 `copilot.suggestion` 是否仍然工作
   - 备选: 可能需要调整 copilot.lua 的配置

2. **自定义键盘映射**
   - 风险: 当前配置有复杂的 Tab 键逻辑
   - 解决: 需要仔细迁移到 blink.cmp 的键盘映射系统

3. **LSP Capabilities**
   - 风险: 某些 LSP 特性可能依赖特定的 capabilities 设置
   - 解决: 确保使用 `blink.cmp.get_lsp_capabilities()`

4. **代码片段兼容性**
   - 风险: 某些自定义代码片段格式可能不兼容
   - 解决: blink.cmp 使用标准的 VSCode snippet 格式

5. **自定义工具函数**
   - 风险: `lua/utils/cmp.lua` 中的函数可能在其他地方被调用
   - 解决: 需要检查是否有其他文件引用这些函数

### 不兼容的功能

1. **nvim-cmp 的 `confirm_done` 事件**
   - 当前用于自动添加括号
   - blink.cmp 有内置的 `auto_brackets` 功能

2. **自定义 snippet 预览和修复**
   - 当前 `utils/cmp.lua` 中的片段处理函数
   - blink.cmp 内置了片段处理

3. **自定义补全源格式化**
   - 当前使用自定义的图标和显示格式
   - blink.cmp 使用 `completion.menu.draw.components`

---

## 🎯 预期收益

迁移到 blink.cmp 后的优势：

1. **性能提升**
   - 更快的补全响应（0.5-4ms）
   - 异步单核心处理
   - 更好的大文件支持

2. **功能增强**
   - 开箱即用，配置更简单
   - 更好的模糊匹配（typo resistant）
   - 内置 frecency 和 proximity bonus
   - 更完善的 LSP 支持

3. **代码简化**
   - 移除 `utils/cmp.lua` (203 行)
   - 简化配置文件
   - 更少的依赖插件

4. **维护性**
   - 活跃维护的项目
   - 更现代的代码库
   - 更好的文档

---

## 📋 检查清单总结

### 开始前
- [ ] 确认已阅读完整任务文档
- [ ] 确认理解迁移步骤
- [ ] 创建备份或 git commit

### 迁移过程
- [ ] 按顺序完成阶段 1-11
- [ ] 每个阶段完成后测试
- [ ] 遇到问题记录并解决

### 完成后
- [ ] 所有功能测试通过
- [ ] 性能符合预期
- [ ] 清理所有备份文件
- [ ] 更新相关文档

---

## 📚 参考资源

- [blink.cmp 官方文档](https://cmp.saghen.dev)
- [blink.cmp GitHub](https://github.com/saghen/blink.cmp)
- [blink.cmp 配置参考](https://cmp.saghen.dev/configuration/reference)
- [社区源列表](https://cmp.saghen.dev/configuration/sources#community-sources)
- [nvim-cmp 迁移讨论](https://github.com/Saghen/blink.cmp/discussions)

---

## 📞 支持

如遇到问题：
1. 查看 blink.cmp GitHub Issues
2. 查看 blink.cmp Discussions
3. 回滚到备份配置

---

**创建时间**: 2024-01-XX  
**预计完成时间**: 迁移后 15-20 分钟  
**最后更新**: 未开始

---

## ✍️ 执行日志

_此部分在执行任务时填写，记录实际操作和遇到的问题_

### 执行记录

- [ ] 开始时间: ___________
- [ ] 完成时间: ___________
- [ ] 实际耗时: ___________
- [ ] 遇到的问题:
  1. 
  2. 
  3. 

### 配置调整记录

_记录实际配置与计划配置的差异_

---

**任务状态**: ⏸️ 等待确认
